package com.airalo.example.offer.presentation.ui.token

import androidx.compose.ui.unit.dp

object Offset {
    data object DropShadow {
        val radius = 30.dp
        val x = 0f
        val y = 10.dp
    }

    val dropShadow = DropShadow
}
